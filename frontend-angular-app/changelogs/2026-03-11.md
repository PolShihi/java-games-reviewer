# Changelog 2026-03-11

- Added Angular sequential plan mirroring the React plan and backend API contract in `frontend-angular-app/frontend-sequential-plan.md`.
- Added React UI + backend API audit for Angular porting in `frontend-angular-app/react-audit.md`.
- Added Angular core infrastructure: API config, HttpClient with error interceptor, models, utilities, and service layer in `frontend-angular-app/src/app/core`.
- Fixed query param typings to include index signatures for HttpClient param builder compatibility.
- Added base URL interceptor and switched API services to relative paths.
- Centralized API endpoints in `api-endpoints.ts` and refactored services to use them.
- Added routing + base layout with placeholder pages for step 3.
- Added Angular Material, enabled animations, and switched layout/NotFound UI to Material components.
- Implemented GamesPage grid, filters, sorting, pagination, and loading/error/empty states using Angular Material.
- Fixed generic typing for fetchAllPageContent to accept typed params in GamesPage.
- Fixed sortDirection type narrowing in GamesPage filter reference loading.
- Installed zone.js and imported it in `src/main.ts` to fix async change detection (initial load and first-click issues).
- Enabled Zone-based change detection via `provideZoneChangeDetection` to ensure UI updates on async operations.
- Root cause: app was effectively running without Zone-based change detection, so async HttpClient updates only rendered after a user event. Fix: enable Zone change detection.
